#include "list.h"
#include "tests.h"
#include <stdio.h>
#include "theRevenant.h"

int main(void) {
    //printf("%d ", listTests());
    bool errorCode = false;
    positionOfTheRevenant(3, 5, &errorCode);
}